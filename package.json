{
  "name": "momentum-ai",
  "version": "0.5.0",
  "private": true,
  "description": "AI-powered Telegram productivity coach",
  "type": "module",
  "scripts": {
    "build": "dotnet fable src --outDir dist",
    "start": "node dist/Index.js",
    "dev": "dotnet fable watch src --outDir dist --run node dist/Index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-cron": "^4.6.0",
    "telegraf": "^4.16.3"
  }
}
