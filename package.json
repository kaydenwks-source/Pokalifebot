{
  "name": "momentum-ai",
  "version": "0.20.0",
  "private": true,
  "description": "AI-powered Telegram productivity coach",
  "type": "module",
  "scripts": {
    "build": "dotnet fable src --outDir dist",
    "start": "node --disable-warning=ExperimentalWarning dist/Index.js",
    "dev": "dotnet fable watch src --outDir dist --run node --disable-warning=ExperimentalWarning dist/Index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-cron": "^4.6.0",
    "telegraf": "^4.16.3"
  }
}
